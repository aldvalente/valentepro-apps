export const metadata = {
  title: 'Gearbnb - Sports Equipment Rental',
  description: 'Rent sports equipment from local owners',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
