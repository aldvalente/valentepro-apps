# Example env vars for running db/init_db.py 
PGHOST=127.0.0.1
PGPORT=5432
PGUSER=postgres
PGPASSWORD=your_password_here
PGDATABASE=apps
# Optional: set DATABASE_URL to a full DSN
# DATABASE_URL=postgresql://user:pass@host:5432/dbname
